/** Number of days after checkout during which a guest can submit a review. */
export const REVIEW_WINDOW_DAYS = 14;

/** Hours after end of checkout day during which a guest can submit or edit a review via reservation code. */
export const GUEST_REVIEW_WINDOW_HOURS = 48;
